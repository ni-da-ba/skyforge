# Skyforge program phase reconciliation — 2026-09-11

**Authority:** project-owner reconciliation of the current convergence boundary  
**Scope:** correct stale Phase-1 / morphology-transition bookkeeping and authorize the next bounded autonomous machine graph  
**Does not replace:** `PROGRAM_ROADMAP.md`, lane ownership, `VALIDATION_POLICY.md`, cross-lane contracts, issue acceptance criteria, or genuine human/manual gates.

## 1. Phase-state correction

The repository and issue record have advanced beyond the older phase table in `PROGRAM_ROADMAP.md`.

Current effective convergence state is:

| Phase | Name | Effective state |
| --- | --- | --- |
| 0 | Core world-synthesis foundation | COMPLETE |
| 1 | Production geography convergence | **COMPLETE** for the transition purpose |
| 2 | Integrated physical world | **PRIMARY_ACTIVE** |
| 3 | Bootstrap Province | **NEXT_AUTHORIZED / bounded preflight active** |
| 4+ | Later complete-game phases | PLANNED unless separately authorized |

This reconciliation is intentionally narrow. Issue #214 may remain open for later integrated/release evidence and concrete defect follow-up without keeping Phase 1 globally active.

## 2. Morphology gates already resolved

The project owner has already resolved the two transition decisions that the older roadmap wording could mistakenly resurface:

- **HS-04 morphology strategy/product decision:** APPROVED / RESOLVED on issue #214. Ordinary traversable geography should predominantly use low-to-medium local relief; dramatic Massif terrain remains valid for exceptional roles; Tableland should read as broader/quieter benches and plateaus; quiet geography should make dramatic geography matter; preserve broad morphology capacity rather than pruning valid difficult terrain.
- **HS-03 post-morphology transition:** the required representative hybrid smoke and representative external-provider smoke were completed and passed. The accepted direction is to stop isolated morphology certification except for concrete defect-driven follow-up and pivot to integrated world systems.

Therefore a stale roadmap node or prose reference must **not** request HS-03/HS-04 again.

## 3. Primary autonomous machine path

The machine-readable execution authority is `ORCHESTRATOR_ROADMAP.json`.

Phase 2 now advances through bounded issue-backed tranches:

```text
#491 base-metal realization
  -> #492 authored visible hydrology
  -> #493 generic structure reintegration
  -> #494 production ecology composition
  -> #496 dressed-region machine proof
  -> human dressed-region review
```

The purpose is not exhaustive subsystem certification. Each task should retire one concrete integration risk and move on.

## 4. Parallel Bootstrap machine path

Already-authorized Bootstrap work may continue in parallel where prerequisites permit:

```text
#466 Bellanca/Guild Content follow-through
#467 civilization runtime follow-through
#237 Portable Engine cutoff
#495 minimum Bootstrap atmosphere
#239 Bellanca B0 automation-safe machine evidence
#497 deterministic Bootstrap Province plan fixture
#498 machine-complete Bootstrap Province integration
```

Low-priority reserve work remains available through #441 (AAL feasibility) and #227 (CC:Tweaked/infrastructure preflight) after higher-value convergence work is unavailable or blocked.

## 5. Human-gate isolation rule

A human gate blocks **only work that actually depends on that judgment**.

Examples:

- dressed-region visual review must not pause unrelated civilization/runtime/atmosphere work;
- Bellanca flight-feel review must not pause unrelated Phase-2 world integration;
- Bootstrap play review must not pause independent low-priority machine reserve work.

The controller may continue selecting any other eligible roadmap node while a gate node is blocked.

## 6. Manual asset-compiler exclusion

Issue #488 / PR #489 and its asset-compiler proof are **not autonomous roadmap work**. The project owner is handling that stream manually.

Autonomous structure, civilization, and Bootstrap tasks must not:

- modify the compiler proof branch/artifacts as part of roadmap authority;
- treat compiler progress as a prerequisite unless a future explicit owner decision adds that dependency;
- seize architectural/art-direction decisions belonging to that manual proof.

## 7. Standing-lane discipline

There is deliberately no standing autonomous make-work authority for Authorship or Audit.

- **Authorship** wakes when a concrete downstream consumer proves an accepted neutral-world semantic is missing or defective.
- **Audit/orchestration** wakes for an observed liveness/safety/efficiency defect or an explicit owner policy change.

Do not manufacture semantic layers, evidence corpora, controller refactors, or infrastructure merely because a producer is idle.

## 8. Autonomous safety envelope

Every roadmap task remains bounded by all of the following:

- current `main`, merged history, source and tests are authoritative;
- one task-owned controller PR in flight at a time;
- provider quota governor and daily roadmap claim ceiling remain authoritative;
- exact lane ownership is preserved;
- machine work cannot decide aesthetics, pacing, play feel, economy tuning, final density, or other human/product questions;
- private/unsafe/guessed retained-mod APIs are not authorized;
- sampled expensive evidence is widened only when a representative exposes a distinct failure class;
- no Phase-4+ broad production work is authorized by this reconciliation.

## 9. Effective routing instruction

When the ordinary controller queue is idle, use the highest-priority eligible node in `ORCHESTRATOR_ROADMAP.json`.

If a task's relevant work is already accepted on current `main`, return the narrowest valid successor/no-op outcome rather than recreating it. If the remaining boundary is genuinely human/manual, surface the gate and continue unrelated eligible work.

This note is the durable semantic reconciliation for the substantial autonomous authority pack introduced after the completed morphology transition.
