package io.github.nidaba.skyforge.kernel.graph;

/** The field-valued result types supported by the initial procedural graph. */
public enum GraphValueType {
    /** A scalar field over the horizontal {@code x-z} plane. */
    SCALAR_FIELD_2,

    /** A scalar field over three-dimensional {@code x-y-z} space. */
    SCALAR_FIELD_3
}
