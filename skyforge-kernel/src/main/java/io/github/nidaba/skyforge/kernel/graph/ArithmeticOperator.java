package io.github.nidaba.skyforge.kernel.graph;

/** Binary scalar-field arithmetic supported by the initial graph model. */
public enum ArithmeticOperator {
    ADD,
    SUBTRACT,
    MULTIPLY,
    DIVIDE
}
