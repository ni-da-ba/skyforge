package io.github.nidaba.skyforge.world;

/** Why an F3E-qualified component is not yet supported by the narrow F4A terrain primitive. */
public enum SkyIslandComponentFluvialTerrainDeferralReason {
    CONFLUENCE_TERRAIN_REQUIRED,
    CASCADE_TERRAIN_REQUIRED,
    PARTIAL_SPAN_TERRAIN_REQUIRED,
    TERMINAL_TERRAIN_REQUIRED
}
