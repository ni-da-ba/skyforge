package io.github.nidaba.skyforge.world;

/** Semantic role of one routed channel corridor within an authored island network. */
public enum SkyIslandChannelRole {
    HEADWATER,
    TRIBUTARY,
    TRUNK
}
