package io.github.nidaba.skyforge.world;

/** AUTH-0056 terminal outcome of exactly one approved candidate execution. */
public enum SkyIslandSupportConvergenceOutcome {
    PLANNER_REJECTED,
    FRESH_SYNTHESIS_INCOMPLETE,
    FRESH_RESERVATION_REJECTED,
    ACCEPTED_ONE_PASS
}
