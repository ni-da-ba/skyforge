package io.github.nidaba.skyforge.world;

/** Direction in which an ordinary hydraulic span meets an explicit transition. */
public enum SkyIslandHydraulicTransitionBoundaryRole {
    INCOMING,
    OUTGOING
}
