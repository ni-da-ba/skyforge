package io.github.nidaba.skyforge.world;

/**
 * Overlapping mesoscale material domains derived from AUTH-0031 continuous material character.
 *
 * <p>These are semantic realization domains, not named rocks, mineral species, ore blocks, or
 * backend palettes.
 */
public enum SkyIslandMaterialDomainKind {
    ALTERED_ZONE,
    SATURATED_BODY,
    MINERALIZED_BODY,
    STRUCTURAL_FABRIC_DOMAIN
}
