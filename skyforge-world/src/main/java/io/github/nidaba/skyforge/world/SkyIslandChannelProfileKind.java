package io.github.nidaba.skyforge.world;

/** Coarse backend-neutral geomorphic profile for an accepted channel reach. */
public enum SkyIslandChannelProfileKind {
    ALLUVIAL,
    INCISED,
    CASCADE
}
