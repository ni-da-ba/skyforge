package io.github.nidaba.skyforge.world;

/** Terminal status for the deterministic bounded hydraulic quadratic solver. */
public enum SkyIslandHydraulicQpStatus {
    SOLVED,
    INFEASIBLE,
    NUMERICAL_FAILURE
}
