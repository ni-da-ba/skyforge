package io.github.nidaba.skyforge.world;

/**
 * AUTH-0064 outcome classes attested by a downstream commit coordinator.
 *
 * <p>These values describe backend-reported outcome only; the authorship layer does not infer them.
 */
public enum SkyIslandPublishedWorldCommitOutcome {
    SUCCEEDED,
    FAILED
}
